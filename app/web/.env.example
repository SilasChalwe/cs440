# mock or http
VITE_BACKEND_API_MODE=mock

# Base backend URL (endpoints are prefixed with /api in code)
VITE_BACKEND_API_BASE_URL=http://localhost:8080

# Request timeout in milliseconds
VITE_BACKEND_API_TIMEOUT_MS=10000
